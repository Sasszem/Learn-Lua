//globals.h

GtkApplication *app;

/*GtkWidget *window;
GtkWidget *program_view, *inst_view, *lines;
GtkTextBuffer *program_buffer, *inst_buffer;

GtkWidget *grid;
GtkWidget *toolbar;
GtkWidget *run_button;
GIcon *run_icon;
*/

#define GTK_CSS_FILE_NAME "./style.css"
#define GTK_GUI_FILE_NAME "gui.glade"

#define SAVE_PATH "./save"

#define LIST_FILE_NAME "/list"
